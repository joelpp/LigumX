[postserialization]
class World
{
	SunLight* SunLight;
	std::vector<Entity*> Entities;

	[transient]
	std::vector<Entity*> DebugEntities;

	[transient]
	std::vector<Sector*> Sectors;

	[setcallback]
	bool Reset = false;
}