[postserialization]
class Texture
{
	std::string Filename;
	bool IsCubeMap;

	[transient]
	GLuint HWObject;

	int NumChannels;

	int BitsPerPixel;

	[enum]
	GLPixelFormat InternalFormat;

	[enum]
	GLPixelFormat Format;

	[enum]
	GLPixelType PixelType;

	glm::ivec2 Size;
}